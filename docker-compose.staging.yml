version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    container_name: borclu-sorgulama-staging
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:/app/prisma/dev.db
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET}
      - NEXTAUTH_URL=${NEXTAUTH_URL:-http://localhost:3000}
      - PORT=3000
      - STAGING_MODE=true
    volumes:
      # Database persistence
      - borclu-db-data-staging:/app/prisma
      # WhatsApp session persistence
      - whatsapp-session-data-staging:/app/.wwebjs_auth
      - whatsapp-cache-data-staging:/app/.wwebjs_cache
      # Temporary upload files
      - upload-temp-data-staging:/app/temp
    networks:
      - borclu-network-staging
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    # Security settings for staging
    security_opt:
      - no-new-privileges:true
    read_only: false
    tmpfs:
      - /tmp:noexec,nosuid,size=100m
    # Resource limits
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '1.0'
        reservations:
          memory: 512M
          cpus: '0.5'

volumes:
  borclu-db-data-staging:
    driver: local
  whatsapp-session-data-staging:
    driver: local
  whatsapp-cache-data-staging:
    driver: local
  upload-temp-data-staging:
    driver: local

networks:
  borclu-network-staging:
    driver: bridge